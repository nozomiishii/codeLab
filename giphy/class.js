console.log("class.js");

class Image {
  constructor(url) {
    this.url = url;
    this.x = mouseX;
    this.y = mouseY;
    this.width = random(10, 20);
    this.height = random(10, 20);
  }
  show() {

  }
}
